//四则运算器的拓展部分，输入指数和底数，进行幂运算并输出结果。
/*#include<stdio.h>      //“标准输入，输出”头文件
#include<stdlib.h>       //标准库头文件
#include<math.h>      //包含math函数的标准库头文件
int main(void)        //有且仅有一个的主函数
{
	int x, y,z = 0;     //定义x,y代表需进行计算的两个初始值，定义z并赋值为0代表计算后得到的结果
	printf("请输入底数和指数：\n"); //打印输出文字提示语句
	scanf_s("%d%d", &x, &y);   //输入x,y 的值
	z = pow(x, y);      //运用math中的幂函数进行计算
	printf("所得结果是%d", z);   //输出结果
}*/